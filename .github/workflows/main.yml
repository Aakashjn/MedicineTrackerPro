# .github/workflows/main.yml
name: Medicine Tracker CI/CD

on:
  push:
    branches:
      - main
      - feature/full-pipeline-setup
  pull_request:
    branches:
      - main

env:
  NODE_VERSION: '20'
  SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }} # Add this

jobs:
  build-and-deploy:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install backend dependencies
        run: npm install

      - name: Run backend tests
        run: npm test
        continue-on-error: true

      # --- Code Analysis ---
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2.2.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          # You may need to configure sonar-project.properties file
          # Or use args parameter for inline configuration
          args: >
            -Dsonar.projectKey=your-project-key
            -Dsonar.organization=your-org
            -Dsonar.sources=./
            -Dsonar.tests=__tests__/
            -Dsonar.test.inclusions=**/*.test.js

      # TODO: Add dependency scans next
      # TODO: Add Docker build
      # TODO: Add image scans  
      # TODO: Add Jenkins trigger
